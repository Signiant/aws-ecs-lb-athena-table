# Functions for working with Athena


def create_athena_table(force, bucket, service_name, session):
    print "create athena table"


def create_athena_database(name, session):
    print "create_athena_database"

    # client = session.client('athena')

    # create database if not exists djn
